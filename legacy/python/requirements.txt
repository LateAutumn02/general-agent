# General Agent - v1 Dependencies
# Phase 1 (Initialization + API): anthropic SDK, config, logging
# Phase 2 (Agent + Tools): core loop, tool execution
# Phase 3 (Compact/Memory/Skills/Sandbox): compression, persistence

# API Clients
anthropic>=0.80.0
openai>=1.0.0

# Data & Config
pydantic>=2.0.0
pyyaml>=6.0

# HTTP / Async
httpx>=0.27.0

# CLI
rich>=13.0.0
click>=8.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Dev
black>=24.0.0
ruff>=0.4.0
