# CLAUDE.md — general-agent 项目指令

## 核心规则

### 提交和推送必须经用户同意
- **commit 前**：展示待提交的文件列表和变更摘要，用户同意后再 commit
- **push 前**：展示待推送的 commit，用户同意后再 push
- commit message 用英文，格式 `<type>: <description>`
- 禁止在 commit message 中写 "Generated by Claude"、"Co-Authored-By" 等工具签名

### 开发流程
1. 从 `dev` 切出 `feature/<功能名>` 分支
2. 先读 `docs/` 对应模块文档 + 参考源码，理解设计后再编码
3. 如果没有设计文档，先补齐文档再写代码
4. 尽量完整保留参考项目的功能，暂时做不了的写好函数骨架并注释 `[v1置空]`
5. 代码完成后跑测试，更新对应 docs 文档
6. 最后向用户汇报：开发进度、测试效果、与参考项目的差异及原因

### 文档规范
- 每个模块两个文件：`flow.md`（流程）+ `data-structure.md`（数据结构）
- 用文档规定的伪代码风格写数据结构，禁止混用 TypeScript/YAML/JSON 语法
- 标注 `[v1]`（需要）和 `[v1置空]`（暂不实现）
- 末尾标注 `最后更新: YYYY-MM-DD | 参考源 commit: <hash>`
