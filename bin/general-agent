#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export GENERAL_AGENT_CALLER_DIR="${GENERAL_AGENT_CALLER_DIR:-$(pwd -W 2>/dev/null || pwd)}"

cd "$ROOT_DIR"
exec bun run ./src/cli/main.tsx "$@"
